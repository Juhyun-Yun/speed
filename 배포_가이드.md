# 누가 가장 빠를까? — 배포 가이드

## ⚠️ 자주 묻는 질문

### 시트의 모둠 값을 바꿨는데 앱에는 컴퓨터와 1:1 경주만 나와요
- ✅ **시트 데이터(학생/모둠) 변경**: **재배포 불필요**. 학생이 페이지 새로고침만 하면 즉시 반영.
- ✅ 시트의 **C열에 모둠 값**이 적혀 있어야 함 (예: 1, 2, 가, 나 등).
- ❌ Apps Script 코드가 **모둠 컬럼을 모르는 옛 버전**이면 안 됨 → 아래 "Apps Script 코드 설치"에 있는 새 `apps_script_template.gs`를 통째로 붙여넣고 **버전 새로 만들기**로 재배포.

### Apps Script 코드를 바꿨을 때만 재배포 필요
- **배포 → 배포 관리 → 연필(편집) → 버전: 새 버전 → 배포** 순서로 누르면 **URL은 그대로 유지**되고 최신 코드가 적용됨.
- 학생들에게 보낸 링크는 바꾸지 않아도 됨.

---


## 시트 구조

- 시트 이름: `학생명단` (Apps Script 코드의 `STUDENT_SHEET_NAME` 상수와 일치해야 함)
- 1행: 제목(번호 | 이름 | 모둠)
- 2행부터 데이터

| A: 번호 | B: 이름 | C: 모둠 |
|---|---|---|
| 1 | 김민준 | 1 |
| 2 | 이서연 | 1 |
| 3 | 박도윤 | 1 |
| 4 | 최지우 | 1 |
| 5 | 송하준 | 2 |
| 6 | 황은우 | 2 |
| 7 | 정유나 |    |  ← 모둠 비어 있음 → 컴퓨터와 경주 |

같은 모둠 값(C열)을 가진 학생들이 같이 경주합니다. 비어 있으면 그 학생은 혼자 → 컴퓨터와 2인 경주.

## Apps Script 코드 설치

1. 시트에서 **확장 프로그램 → Apps Script** 클릭
2. 기본 `Code.gs`의 내용을 **모두 지우고**, `apps_script_template.gs`의 전체 내용을 붙여넣기
3. 왼쪽 파일 트리에서 **+ → HTML**로 새 HTML 파일을 만들고 이름을 `index`로 지정
4. 만들어진 `index.html`의 내용을 모두 지우고, 이 폴더의 `index.html` 전체 내용을 붙여넣기
5. 💾 저장

## 웹 앱 배포

1. **배포 → 새 배포**
2. 톱니바퀴(⚙️) → 유형: **웹 앱**
3. 설명: 아무거나 (예: `v1`)
4. 다음 사용자 권한으로 실행: **본인 (자기 학교 계정)**
5. 액세스 권한: **모든 사용자** (학생들이 로그인 없이 접근하기 위함)
6. **배포** 클릭 → 권한 허용
7. 배포 완료 후 표시되는 **웹 앱 URL**을 복사 → 학생들에게 공유

## 다른 선생님이 사용할 때

1. 이 시트를 **사본 만들기** (사본을 만들면 Apps Script도 같이 복제됨)
2. 사본의 시트 이름이 `학생명단`인지 확인하고, C열에 본인 반 모둠 구성을 입력
3. 위의 "웹 앱 배포" 절차대로 **새 배포**를 만들어 본인의 URL을 얻음
4. 그 URL을 본인 반 학생들에게 공유

## 동작 방식

### 경주 모드 자동 판정
- 학생이 자기 이름을 선택 → 시트에서 그 학생의 **모둠** 값 조회
- 모둠 값이 있으면 → 같은 모둠의 다른 학생들과 **실시간 경주**
- 모둠 값이 없거나 같은 모둠에 학생이 혼자뿐이면 → **컴퓨터와 2인 경주**

### 실시간 동기화 원리
- 각 학생 기기가 약 350ms마다 Apps Script에 GET 요청 → 자기 진행도 보고 + 모두의 상태 받음
- 서버는 `CacheService`에 모둠별 경주 상태(`race:<모둠>:<게임종류>`)를 저장 (TTL 10분)
- 누구든 **시작** 버튼을 누르면 → 같은 모둠 전원에게 3초 카운트다운 → 동시 시작
- 4초 이상 응답 없는 학생은 이탈로 표시(다른 학생들은 계속 경주 진행)

### 게임 종료
- **같은 시간 동안 누가 더 멀리?**: 10초 후 자동 종료 → 거리 비교
- **같은 거리를 누가 더 빨리?**: 모두 50m 도달 시 또는 30초 경과 시 종료 → 시간 비교

## 비용/한계

- 모두 무료 (Apps Script 일일 트리거/요청 한도 내)
- 동시 요청 한도: 4-5명 모둠 정도는 무리 없음
- 통신 지연: 평균 ~500ms (Apps Script 응답 속도 의존, 가끔 1초 넘어가는 경우도 있음)
- 같은 모둠에 같은 게임을 동시에 여러 번 시작하면 충돌 가능 → 한 라운드 끝나고 다시 하기 권장

## 학생 안내

- 자기 이름 선택 → 메뉴 → **개념 확인** 클릭
- **같은 시간 동안 누가 더 멀리 갔을까요?** 화면에서 모둠 친구가 모두 입장하면 누구든 ▶ 시작 누르기
- 3-2-1 카운트다운 후 다 같이 출발
- **스페이스바를 빠르게 두드려서** 자기 캐릭터를 달리게 함 (한 번 누름 = 1m)
- 결과 확인 후 → **다음 게임 ▶** → **같은 거리를 누가 더 빨리 갔을까요?**도 같은 방식
